# Fireflydle Context

本文记录 Fireflydle v1.0 规划中已经确认的领域词汇，避免后续讨论用同一个词表达不同的发布范围。

## Language

**v1.0**:
Fireflydle 对玩家正式承诺的完整产品版本：题库完整、规则可解释、包含已验收的 NPC 题池，并具备长期复玩与社交传播闭环。NPC 题池未完成并通过验收前，不发布 v1.0。
_Avoid_: MVP、功能基本齐全版、先发后补版

**NPC 题池**:
经过 Fireflydle 审核、可作为题目候选的非可玩剧情人物集合；官方 NPC 图鉴中的全部条目不自动等于 NPC 题池成员。
_Avoid_: NPC 全量、NPC 图鉴、路人角色库

**NPC 完成标准**:
v1.0 采用审核型正式白名单作为 NPC 的完成对象，不要求官方 NPC 候选全集无差别入池。每个答案 NPC 必须有可追溯的官方来源、至少一个可用名称及其它语言的回退显示、至少两条公平可推理的判题事实，以及明确的首次登场版本或等价证据，并通过一次人工确认。具体目标规模在候选清点后按实际规模确定；24–36 个核心、60–100 个专家仅作为规划参考，不是硬性数量门槛。已确定目标名单内的条目全部可正常游玩后才算完成。
_Avoid_: 官方 NPC 全量自动入池、只凭名称入池、未审核题池

**发布阻塞项**:
必须在 v1.0 发布前完成并通过验收的条件；它可以不阻塞前置开发，但未完成时不能将版本标记为 v1.0。
_Avoid_: 后续优化、可选功能、发布后补丁

**v1.0 完整范围**:
Roadmap 从 v0.4 到 v1.0 是正式发布范围的基线；后续访谈明确删除或替换的模式分层、同角色异形态提示、专题筛选、赛季与段位不再属于 v1.0。其余确认能力可以分阶段完成，但不能留到 v1.0 之后。
_Avoid_: 忽略后续明确决策机械照搬 Roadmap、先发核心以后补功能、部分完成版

**Roadmap 全量完成**:
“全部”按 Roadmap 的明确产品能力、发布项和必要质量要求解释；实现建议按小站实际风险取舍，不把 P2 优化、特定迁移套路或内部工具形态自动升级为 v1.0 发布阻塞项。
_Avoid_: 把建议性工程方案当硬性产品范围、为了形式完成技术工作、只完成用户可见能力

**NPC 模式**:
使用独立 NPC 题池和判题字段，不把 NPC 直接混入每日题或普通角色模式；它仍然属于 v1.0 完整范围。
_Avoid_: NPC 混合池、NPC 全量图鉴、默认每日 NPC

**v1.0 模式题池矩阵**:
v1.0 按内容分为普通角色、NPC、货币战争和星神四种模式，不再按入门、标准、完整划分可玩角色层级。普通角色模式包含全部独立可玩实体；其它三种模式分别使用自己的目标池、候选池和字段系统。每日题默认使用普通角色模式；无尽、好友挑战、私人房和周赛必须显式选择内容模式，不隐式混池。
_Avoid_: 按难度复制可玩角色题池、每日题默认特殊模式、不同模式共用不兼容字段

**内容模式入口**:
游戏主界面顶部使用普通角色、NPC、货币战争、星神四个标签，默认进入普通角色；切换后只显示该模式支持的玩法。普通角色提供每日题、练习、无尽、实时匹配、好友挑战和私人房；其它三种提供练习、无尽、好友挑战和私人房；当周轮到的内容模式额外显示周赛入口。
_Avoid_: 不可用入口占位、四种模式拆成四套网站页面、特殊模式默认进入每日题

**星神题池边界**:
星神属于 v1.0 独立内容范围，使用 `aeon` 图片猜测题池，不归入 NPC 白名单，也不复用可玩角色的五字段规则。敌人、强敌和剧情首领不做题池，不属于 v1.0 范围。
_Avoid_: 星神当 NPC、敌人或首领模式、跨实体类型强行补齐字段

**集中发布**:
功能在内部按里程碑和 RC 分阶段集成、测试，所有 v1.0 范围能力完成并验收后，再统一对外标记为 v1.0。
_Avoid_: 功能完成即单独升版、边做边发正式版

**v1.0 功能完成验收**:
Roadmap 中每一项能力必须已实现并接入正式用户流程，验收标准可重复执行，并通过与风险匹配的单元、集成和端到端验证；还要完成简体中文、English、日本語及移动端/桌面端的基本验证。数据类能力必须带来源、版本和审核记录；不得存在阻塞游戏、泄题、答案不一致、题池污染或严重数据错误。
_Avoid_: 只有接口或隐藏开关、只在开发环境验证、缺少数据溯源

**v1.0 已知问题门槛**:
允许保留已记录、可规避且不影响核心游戏公平性和主要流程的低严重度问题；不允许存在阻塞问题、严重数据错误、跨语言答案不一致、NPC 审核缺失或核心模式不可用。
_Avoid_: 带已知公平性问题发布、用后续补丁掩盖发布阻塞项

**无赛季与段位**:
v1.0 不做赛季、青铜至钻石等段位、定级赛、晋级机制、赛季徽章或结算奖励。现有 ELO 仅作为普通角色实时匹配评分和常驻排行榜数据持续使用，不按周期重置；界面显示纯数字 ELO、每局变化值和常驻排名，不包装成段位名称或进度条。
_Avoid_: 为 ELO 包装段位层级、建立赛季周期和结算任务、引入段位奖励经济

**每周公共挑战**:
每周提供一个独立于每日题的全球统一挑战，按普通角色、NPC、货币战争、星神四种内容模式逐周固定轮换，然后循环。每周固定连续 5 题，所有玩家使用相同题序、题池和规则；不再增加地区、版本、星级等专题筛选。排行榜按完成情况、总猜测次数和总耗时排序，结果可分享但不泄露未完成题目。
_Avoid_: 玩家各自抽题却共用排行榜、每周挑战泄露后续答案

**周赛成绩规则**:
一题失败后继续完成剩余题目，不终止整周挑战。每个账号每周只有第一次挑战计入排行榜，依次按猜中题数更多、总猜测次数更少、总用时更短排序；失败题按该模式最大猜测次数计入。完成后可重玩练习但不覆盖首次成绩；游客可游玩和分享，不进入全球榜。
_Avoid_: 一题失败直接结束、重玩覆盖正式成绩、游客匿名刷全球榜

**周赛时间边界**:
周赛使用北京时间（UTC+8），每周一 `00:00` 开始并在下周一 `00:00` 结束，页面显示剩余时间。周赛创建后固定当周题目和 manifest 版本，期间题库更新不改变已开始的周赛。
_Avoid_: 每日与周赛使用不同地区日期、周中热更新题目、客户端本地时区决定周次

**个性化每日题**:
每日题继续按北京时间日期、用户 ID 和当日题目锚点确定个人答案；同一天不同玩家可以得到不同普通角色，同一账号当天答案保持不变。游客按本地访客身份生成个人答案。每日分享不得泄露答案。
_Avoid_: 所有人固定同一每日答案、刷新页面更换个人答案、分享内容直接剧透

**每日题统计范围**:
个性化每日题不设全球排名，只展示个人完成记录、连续天数、猜测分布和当天全站完成人数。同题公平竞争由每周公共挑战承载，不为每日不同答案计算角色难度系数。
_Avoid_: 不同答案按猜测次数直接排名、为每日榜建立复杂难度校正、每日统计泄露个人答案

**v1.0 成就范围**:
必须交付六类有明确行为意义的成就：一发入魂、连续 7 日完成每日题、取得 10 连胜、最后一次可用猜测命中、首次完成 NPC 模式、累计完成 100 局。允许后续扩展，但不以单纯点击次数或游玩次数堆叠无意义成就。
_Avoid_: 只做展示占位、无行为意义的数字成就

**PWA 离线边界**:
离线支持应用 Shell、静态资源、已发布题库和四种内容模式的本地练习；每日题、好友挑战、排行榜、周赛和多人对战仍要求联网。离线成绩与历史只保存在本机，恢复网络后不回传账号，也不计入统计、成就或排行榜。
_Avoid_: 离线伪造每日题/排行榜、建立离线成绩同步队列、离线状态覆盖在线成绩

**PWA 离线包获取**:
安装时只缓存应用外壳和普通角色离线包；NPC、货币战争和星神在玩家首次在线进入对应模式时自动缓存，并显示“可离线使用”状态。尚未缓存的模式离线时置灰并提示需要联网打开一次，不在首次安装时下载全部图片和题库。
_Avoid_: 首装强制下载所有特殊模式资源、未缓存模式离线进入空白页、无状态提示的后台大下载

**添加到主屏幕引导**:
只对具备安装条件且尚未以独立应用运行的移动端用户主动展示。用户首次完成任意一局后，可以在结算页出现一次底部引导；对局进行中、首次进入网站和关键操作期间不得弹出。Android 等支持原生安装事件的平台调用系统安装窗口，iOS 展示“分享→添加到主屏幕”的简短分步说明。用户关闭后 30 天内不再主动提示，移动端菜单长期保留“安装到主屏幕”入口。
_Avoid_: 首次访问立即索要安装、遮挡进行中的对局、桌面端显示移动安装提示、用户拒绝后频繁追问

**整体动效语言**:
沿用现有列车终端、轨道信号、金色与青色能量反馈，不重做信息架构或品牌风格。首页可以使用少量低透明度萤火粒子和轨道信号作为环境动效；模式切换使用滑动指示线与短暂淡入位移；动效重点放在提交猜测、逐格揭示、结果判定、胜负结算、星神遮罩、无尽生命和多人状态变化。
_Avoid_: 把产品页改成宣传页、与现有视觉无关的动效风格、游戏棋盘常驻大量粒子、没有状态含义的装饰动画

**猜测与结算动效**:
提交后结果格按字段顺序依次揭示；`exact` 触发一次绿色光扫，`close` 触发一次金色扫描纹，版本方向箭头只轻推一次，`miss` 快速沉降。猜中时整张棋盘播放一次金青色轨道能量通过后展开结算；猜错仅使用边缘红光和轻微位移反馈。星神遮罩格交错翻开，无尽模式失去生命时对应生命标记碎裂熄灭；PvP 等待、对手提交和双方确认跳过使用克制的雷达脉冲。
_Avoid_: 全屏剧烈震动、持续循环判定动画、大量彩纸遮挡结果、动效结束前阻止下一步操作

**动效强度与减少动态效果**:
默认动效强度按 7/10 设计，明显增强操作反馈和结算仪式感，但不做滚动劫持、全页视差、持续闪烁或猜题页常驻粒子。自动尊重系统 `prefers-reduced-motion`：关闭粒子、交错揭示和位移动画，仅保留极短淡入，不在 v1.0 额外增加站内动效设置开关。页面隐藏或失焦时暂停非必要循环动画。
_Avoid_: 为炫酷牺牲连续游玩效率、忽略系统低动态偏好、后台页面继续运行装饰动画

**声音与移动端触觉**:
v1.0 不加入背景音乐和操作音效。移动端浏览器支持且系统允许时，可在提交、猜中和失去生命三个时刻提供很轻的短触觉反馈；触觉不可作为唯一反馈，不支持时静默跳过，也不得阻塞玩法。
_Avoid_: 默认播放声音、长时间或连续振动、把触觉能力当作玩法前提、为不支持的平台增加报错提示

**题库更新与历史稳定性**:
题库使用带版本号的 manifest。纠错可以发布数据补丁，但已经开始的每日题、周赛和好友挑战继续绑定原题库版本，历史结果不因后续修正改变；新增角色或 NPC 进入后续题库版本，不回溯污染既有成绩。
_Avoid_: 热更新改变进行中的答案、补丁重算历史成绩、回溯加入新题

**正式 ELO 计分范围**:
只有规则固定、题池和计分一致的普通角色随机匹配影响 ELO；私人房、好友挑战、无尽、Modifier、周赛、NPC、货币战争和星神模式不影响正式 ELO，可各自拥有排行榜或成就。
_Avoid_: 特殊规则混入正式 ELO、不同内容模式直接比较评分

**NPC 多人玩法**:
NPC 题池允许用于私人房、好友挑战和周赛轮换，但必须由创建者或挑战链接显式指定 NPC 模式；不进入随机匹配和默认多人房。参与者使用相同题池、规则和字段说明。
_Avoid_: 随机匹配隐式 NPC、参与者题池不一致

**实时随机匹配范围**:
正式实时随机匹配只开放普通角色模式，保持玩家之间的题池、字段和计分一致；NPC、货币战争和星神模式通过单人练习、私人房、好友挑战或周赛使用。
_Avoid_: 不同内容模式混用同一 ELO 队列、随机匹配隐式进入特殊题池

**特殊模式在线入口**:
v1.0 不为 NPC、货币战争或星神新增随机匹配队列；三者分别支持已确认的单人、私人房、好友挑战和周赛入口。
_Avoid_: 为低活跃特殊题池强行开队列、影响普通角色匹配等待时间

## Implementation Decisions

**统一实体模型**:
普通世界观题池使用可判别联合类型 `GameEntity`，包含 `playable`、`npc` 和 `aeon` 三类实体。货币战争使用独立 `CurrencyWarsUnit`；游戏引擎通过题池规则接收对应猜测对象，不把币战单位塞进 `GameEntity`。
_Avoid_: 把所有实体塞进可玩角色 schema、用空值伪造不存在的属性

**数据生成与运行时来源**:
继续使用“官方来源/人工覆盖 → 一个可复现的同步脚本 → 带版本号的静态 manifest → `game-data` 包与 API/前端运行时”的轻量链路。运行时不直接请求 HoYoWiki；只有确有需要时才把 manifest 写入 D1，不为此新增独立数据服务。
_Avoid_: 运行时依赖 Wiki、未版本化的热数据、无法复现历史题库

**题池成员表示**:
普通 `GameEntity` 使用 `targetPools` 替代全局 `targetEligible` 布尔值；`CurrencyWarsUnit` 在自己的 ruleset 中声明币战池成员。每个内容模式声明数据源、允许类型和判题字段；首批题池为 `character`、`npc`、`currency-wars` 和 `aeon`。
_Avoid_: 全局布尔开关、题池逻辑散落在各个调用方、为新增实体类型修改核心成员字段

**GameEntity Schema**:
采用“公共信封 + 类型专属 payload”：公共字段包括 `id`、`kind`、本地化名称/别名及回退值、资源、`targetPools`、来源版本和启用状态；`payload` 按 `playable`、`npc` 和 `aeon` 使用不同字段，不把所有类型的字段做成可选大杂烩。
_Avoid_: 空值伪造字段、类型判断散落在 UI 和 API、实体 schema 失去可校验性

**缺失字段反馈**:
判题反馈新增 `unavailable` 状态，与 `exact`、`close`、`miss` 分开；仅当题池和实体双方都有可比较值时才产生 `miss`。界面用中性占位显示 `unavailable`，题池配置决定必需字段。
_Avoid_: 缺失字段当作 miss、为 NPC 强行补齐可玩属性、不同题池共用固定五格反馈

**统一实体持久化**:
优先在现有角色数据链路上扩展 `GameEntity` 所需字段；只有现有表无法承载 NPC 和星神时才新增统一 `entities` 表。无论采用哪种存储，运行时只能有一个事实来源，对局保存题池和 manifest 版本即可，不要求为每类实体建立独立持久化系统。
_Avoid_: 长期双写两套事实来源、DO 使用未版本化的隐式题池、迁移后继续依赖旧角色表

**题池规则实现**:
采用类型安全的题池注册表与可配置字段定义。字段比较器由 TypeScript 实现，接近阈值、版本方向和阵营关系等参数由题池配置提供；数据文件不能注入任意可执行规则。
_Avoid_: 规则散落在调用方、任意脚本执行、不同模式复制一套比较逻辑

**API 题池选择**:
游戏创建 API 分开接收内容 `modeId` 与玩法 `activityId`：`modeId` 首批为 `character`、`npc`、`currency-wars`、`aeon`，`activityId` 表示 `daily`、`random`、`weekly`、`survival`、好友挑战或私人房等入口。旧 `daily`/`random` 请求暂映射到 `character`；响应返回两者和 `manifestVersion`。
_Avoid_: 把每日或好友挑战当内容题池、客户端自行过滤、未识别模式静默回退

**对局快照绑定**:
对局创建时固定 `modeId`、`activityId`、`poolRuleVersion`、`manifestVersion`、答案实体 ID 和答案实体 payload。每日题、周赛、好友挑战和 Durable Object 房间均使用该快照，后续题库更新不改变已创建对局的判题结果。
_Avoid_: 热更新改变进行中的判题、历史对局按最新 schema 重算

**同步脚本架构**:
使用一个主要同步入口，内部按需要放置可玩角色、NPC 和星神的小型来源函数；统一完成基本校验和 manifest 产出，不预先搭建复杂插件式同步框架。
_Avoid_: 所有来源逻辑堆成不可读文件、为四个固定来源设计通用同步平台

**人工覆盖文件**:
使用少量可读的静态数据文件维护人工覆盖和题池成员；只在单个文件明显难以维护时再按实体类型拆分。关键字段做 Schema 校验，不要求预先固定五份配置文件。
_Avoid_: 无结构大 JSON、为了形式拆分大量小文件、覆盖文件完全绕过校验

**永久实体 ID**:
使用项目自有永久 ID；实体首次入库后不因显示名、跨语言名称或来源页面变化而重命名。来源 ID 变化通过 alias/override 映射，历史对局只引用永久 ID 和快照。
_Avoid_: 用显示名当主键、来源页面 ID 直接暴露为内部 ID、改名破坏历史记录

**前后端实体契约**:
普通题池使用 `EntitySummary`，币战题池使用轻量 `CurrencyWarsUnitSummary`；二者通过带 `modeId`/`poolRuleVersion` 的 `GuessResult` 进入同一游戏流程。旧 `CharacterSummary` 只作迁移期兼容，不作为新 API 的事实模型。
_Avoid_: 新旧 summary 并行成为两套真相、按实体类型丢失必要字段

**动态字段 UI**:
前端按 `PoolDefinition.fields` 动态渲染猜测列。字段定义提供稳定 ID、三语标签、顺序、反馈展示方式和 `unavailable` 支持；不再硬编码五项可玩角色字段。
_Avoid_: 每新增题池就复制一套 GuessBoard、字段顺序由客户端猜测

**统一搜索索引**:
所有题池实体在数据生成阶段产出规范化搜索词，覆盖三语名称、官方别称、审核后的常见简称、拼音、拼音首字母和英文简称。v1.0 不做编辑距离或智能错字模糊匹配；搜索结果依次按当前语言名称完全匹配、名称前缀、搜索词完全匹配、搜索词前缀和普通子串排序，并以稳定实体 ID 作为最终排序依据。
_Avoid_: 运行时重复生成拼音、未经审核自动收集社区称呼、模糊算法导致结果顺序漂移

**搜索命中说明**:
搜索结果始终以当前语言名称作为主标题；查询没有直接命中该名称时，在下方显示一条弱化的本次命中词，例如“匹配：Firefly”。别名、拼音和首字母命中采用相同展示方式，只显示实际命中的已审核搜索词，不展开完整别名列表。
_Avoid_: 搜到别名却无法解释结果、用别名替换正式显示名、结果项堆叠所有搜索词

**客户端兼容**:
前端与 API 按同一发布批次更新；仅保留不会增加明显复杂度的旧请求兼容。新题池需要新字段时，旧客户端直接提示刷新或升级，不维护长期双轨协议。
_Avoid_: 旧缓存静默产生错误结果、为兼容旧 UI 牺牲新题池语义

**数据迁移顺序**:
若确需从 `characters` 迁移到 `entities`，采用一次可回滚的增量迁移并保留旧表备份；迁移完成后切换唯一读写来源，不要求长期维护完整的 expand/backfill/contract 流程。
_Avoid_: 单次破坏性替换、迁移期间双重事实来源、删除历史外键依赖

**发布版本一致性**:
静态题库和 API 使用同一个可读的 `manifestVersion`；发布前做一次构建与冒烟检查，确保创建的新对局使用已发布题库。无需为每个部署组件建立独立版本协调系统。
_Avoid_: 前端新题库配旧 API、DO 快照无版本、部署顺序造成半发布

**题库客户端缓存**:
实体搜索摘要与发布 manifest 绑定，TanStack Query 使用 `staleTime: Infinity`；请求 URL 或查询键包含 `manifestVersion`，新发布通过版本变化自然失效，不轮询不变的题库接口。
_Avoid_: 每次进入游戏重新下载题库、用短时缓存轮询静态数据、版本更新后继续读取旧摘要

**头像资源规格**:
同步流程为普通角色、NPC、币战单位和星神搜索头像生成约 40/80/160px 的 AVIF/WebP 响应式版本，并保留必要回退；列表按实际显示尺寸使用 `srcset` 和懒加载，当前答案或首屏资源可提高加载优先级。以头像总体传输量相对现状下降至少 50% 为验收目标。
_Avoid_: 搜索列表下载原始大图、只改压缩率不生成尺寸、首屏关键头像全部懒加载

**离线数据与代码拆包**:
在线流程只加载当前模式的搜索摘要；完整判题数据仅在玩家主动进入离线练习时动态加载。Admin、Markdown 公告渲染、分享图生成和各模式非首屏代码使用路由或功能级动态导入，不为追求细碎指标拆分核心猜测流程。
_Avoid_: 首屏打包四种完整离线题库、后台代码进入玩家主包、把核心交互拆成大量微小 chunk

**发布失败处理**:
题库生成、构建或核心冒烟测试失败时不发布新版本，继续使用上一份可用题库；修复后重新生成并发布，不要求额外的多环境编排系统。
_Avoid_: 半发布、失败后重新抓取不同上游数据、线上版本不可复现

**多题池测试范围**:
覆盖实体/网络 Schema、字段比较器、题库生成、对局创建和主要 Web 流程；每个题池至少有一组可人工审阅的规则样例和一条端到端通路，不强制拆成五层测试体系。
_Avoid_: 只测 happy path、只有 UI 快照、数据生成器无测试

**判题黄金样例**:
每个题池维护可人工审阅的 golden fixtures，覆盖 `exact`、`close`、`miss`、`unavailable`、版本方向、阵营关系和跨实体限制；规则调整必须显式更新样例。
_Avoid_: 判题快照静默变化、只靠线上玩家发现规则回归

**数据 CI 门禁**:
永久 ID 重复、题池不存在、答案必需字段缺失、没有来源、目标池为空或规则字段与实体不兼容时阻止发布。语言缺失优先使用回退名称；资源哈希和跨组件版本一致性不作为独立硬阻塞。
_Avoid_: 带警告继续生成、发布部分题库、版本不一致仍上线

**NPC 内容工作启动**:
NPC 候选清点、字段可用性统计和审核从第一实施阶段并行启动，以实际规模尽早验证 Schema 与题池规则；在统一实体链路完成前不进入正式运行时。
_Avoid_: 引擎全部完成后才发现 NPC 字段不可用、未审核数据提前入池

**功能开关策略**:
v1.0 实现不要求引入专门 feature flags。未完成能力不得以半成品正式入口发布；使用正常分支、内部里程碑和 RC 管理集成与验收。
_Avoid_: 为一次性迁移长期保留开关、生产出现两套行为路径

**Roadmap 实现顺序**:
严格沿用原 Roadmap 的 v0.4→v1.0 版本轴：v0.4 完成实体/题池地基、搜索、阵营/地区/羁绊、规则页中的 close 解释和明确的性能项；v0.5 完成四种内容模式地基、动态 GuessBoard、独立可玩实体、教学、复盘和原生分享；v0.6 完成 NPC 与星神的同步及独立题池；v0.7 完成好友挑战和分享闭环；v0.8 完成无尽与跳过；v0.9 完成自定义房和 Modifier；v1.0 完成 PWA、周赛、成就及全量验收。赛季、段位和专题筛选经产品确认从范围删除。NPC 候选清点从 v0.4 同期启动，但正式入池归属 v0.6；底层迁移按实际需要完成。
_Avoid_: 另起一套脱离 Roadmap 的产品里程碑、把某种技术实现当产品发布顺序、NPC 清点拖到 v0.6 才开始

**唯一地区字段**:
每个实体只使用一个规范化 `regionId` 作为判题主地区。出生地、曾活动地和剧情关联地点不形成地区数组，必要时仅写入不参与判题的来源备注，避免一个实体因多地点关联产生泛化的 `close`。
_Avoid_: 多地区数组参与默认判题、把出现地点当主地区、地区关系无限扩张

**货币战争羁绊字段**:
《货币战争》羁绊只用于独立 `currency-wars` 模式，不作为普通角色或 NPC 字段；币战私人房必须显式选择 `currency-wars` 内容模式。
_Avoid_: 普通模式增加币战羁绊、把币战做成通用 Modifier、不同题池共用羁绊判题

**货币战争羁绊反馈**:
羁绊集合完全一致为 `exact`，至少共享一个但集合不完全一致为 `close`，双方有数据但无交集为 `miss`，没有官方数据为 `unavailable`。反馈不展示共享羁绊的具体名称，避免泄露额外答案信息。
_Avoid_: close 直接列出共享羁绊、无数据当 miss、只比较单个羁绊

**货币战争费用反馈**:
费用使用 1–5 的有序单值：相同为 `exact`，目标费用更高或更低时只提供方向提示，不把相邻费用判为 `close`。
_Avoid_: 自定义相邻费用 close、把费用方向写成普通版本字段、显示完整候选范围

**货币战争站位反馈**:
站位只使用 `exact` 或 `miss`。前台、后台和前后台分别作为单值枚举；前后台与单一前台/后台之间不额外定义兼容 `close`。
_Avoid_: 把前后台拆成两个值、未经说明的兼容 close、用普通战斗位置替代币战站位

**货币战争羁绊显示**:
羁绊作为一个多值字段展示和判题，内部保留阵营、流派和独立类型用于数据分组；不拆成多个 GuessBoard 列，也不在反馈中显示共享羁绊名称、数量或类型。
_Avoid_: 从羁绊类型或数量侧面泄露答案、把同一集合复制成多列、显示共享羁绊名称

**在线实体信息暴露**:
在线搜索接口只返回实体 ID、名称、别名和头像；属性、羁绊及其他判题字段仅在玩家提交该实体后随 `GuessResult` 返回。答案完整 payload 只存在服务端对局快照。离线练习可以包含完整本地题库，但成绩不进入在线统计。
_Avoid_: 在线下载完整答案字段、客户端自行裁决正式对局、离线成绩进入排行

**货币战争 close 说明**:
货币战争规则页统一说明：羁绊反馈为 `close` 表示双方至少共享一个羁绊。对局响应和单元格不返回羁绊交集的 ID、名称、数量或类型，也不提供逐格原因浮层。
_Avoid_: 原因解释泄露共享羁绊、通过网络响应暴露交集详情、黄色格增加信息按钮

**货币战争字段隔离**:
货币战争费用、站位、定位和羁绊属于独立玩法规则，不是角色的永久世界观属性。v1.0 在 `currency-wars` 模式交付费用、站位和原生羁绊集合，使用独立 `currencyWarsBondIds` 和 `currencyWarsRulesetId`；不放入通用 `affiliationIds`，也不参与普通阵营 `close`。
_Avoid_: 把玩法羁绊当通用阵营、原地覆盖角色永久属性、跨 ruleset 比较

**羁绊术语边界**:
Fireflydle 中“羁绊”仅指《货币战争》的原生羁绊。普通世界观数据不设置通用 `affiliationIds`、“羁绊”字段或身份标签；石心十人、自灭者等应归入派系节点，并与货币战争规则完全隔离。
_Avoid_: 世界观羁绊、通用 affiliation 羁绊、用同一关系同时参与阵营和币战判题

**普通世界观分类**:
普通世界观只使用唯一主叙事地区与唯一主派系，不设置身份标签。无名客、自灭者、石心十人等统一视为派系候选；官方原始“阵营”“身份”“派系”字段只作为归类证据，不直接参与判题。
_Avoid_: 身份标签、把官方混合阵营值直接当地区或派系、一个实体多个主地区

**世界观接近规则**:
地区仅有 `exact`、`miss`、`unavailable`，不设 `close`；派系按固定两层模型比较：`factionId` 相同为 `exact`，不同但 `factionGroupId` 相同为 `close`，其余为 `miss`。
_Avoid_: 同属银河即 close、任意深度阵营图、逐角色维护特殊 close 配对

**版本接近规则**:
实装或首次剧情登场版本按发布序位比较：同一版本为 `exact`，相差 1–2 个版本序位为 `close`，超过 2 个为 `miss`；版本不同始终用方向提示答案更早或更晚。跨 `1.x`、`2.x` 等大版本仍按连续发布序位计算。
_Avoid_: 按月份推算距离、同一大版本全部 close、移除已有早晚方向反馈

**可玩角色与 NPC 题池隔离**:
可玩角色与 NPC 使用不同的目标池和候选池，不在同一局中混合，也不进行跨类型判题。字段覆盖率、准入和验收按题池分别计算；某一池的缺失字段不影响另一池的字段配置。
_Avoid_: 角色与 NPC 混合候选、跨类型比较、用全体实体覆盖率决定单一题池

**普通角色模式**:
普通角色模式收录全部独立可玩实体，使用属性、命途、稀有度、主派系、主叙事地区和实装版本六项字段。它不再拆分入门、标准或完整题池，也不新增身份、普通世界观羁绊或主观标签。
_Avoid_: 按难度复制角色池、排除独立可玩形态、普通角色模式混入币战字段

**普通角色猜测次数**:
普通角色模式固定最多 6 次猜测；每日题、随机练习和正式匹配使用同一规则，不再提供休闲 8 次或困难 4 次的难度选择。私人房的玩法差异由已确认的 Modifier 表达，不重新引入难度档位。
_Avoid_: 内容模式内再套难度层级、每日与随机使用不同次数、把猜测次数包装成新题池

**可玩形态独立实体**:
每个可玩角色或形态都是具有独立永久 ID 的完整答案实体，不设置 `baseCharacterId`、`variantGroupId` 或同人物分组，也不提供“同角色异形态”特殊反馈。开拓者不同命途等形态只按各自六个字段正常比较。
_Avoid_: 根据名称推断人物关系、同人物提示绕过普通判题、把某个形态指定为其它形态的本体

**NPC 目标池与候选池**:
NPC 模式的答案和玩家猜测都只能来自 NPC 实体。NPC 候选池可以大于目标池：只有资料足够、辨识度合适且经人工确认的 NPC 可以成为答案；其它资料可用的 NPC 可以仅作为可猜候选。两份列表保存在同一份版本化 manifest 中。
_Avoid_: 猜可玩角色排除 NPC、候选池外实体可提交、把所有可猜 NPC 自动设为答案

**NPC v1.0 判题字段**:
NPC 模式使用主叙事地区、主派系和首次剧情登场版本；这三项必须对当前目标和候选可判定。`npcType` 和 `debutRoute` 只保留为可选策展数据，不加入 v1.0 GuessBoard，也不作为发布阻塞项。
_Avoid_: 为了字段数量强行缩小 NPC 池、直接复用可玩角色字段、用缺失值伪装完整数据

**NPC 模式猜测规则**:
NPC 模式固定最多 4 次猜测；难度来自 NPC 题池和字段，不新增生命、计时或特殊失败机制。
_Avoid_: 为 NPC 单独设计一套游戏循环、用额外惩罚掩盖题库不足

**NPC 模式首发字段**:
NPC 模式首发 GuessBoard 只展示主叙事地区、主派系和首次剧情登场版本，继续使用现有 `exact`、`close`、`miss` 与版本方向反馈。`npcType` 和 `debutRoute` 仅保留为数据备注。
_Avoid_: 首发加入未经验证的字段、为 NPC 复制一套不同反馈语义

**NPC 模式流程复用**:
NPC 模式复用现有游戏创建、猜测提交、回放和分享接口，只通过 `modeId: "npc"` 切换实体列表和字段；不新建 NPC 专用 API、Durable Object 或页面。
_Avoid_: NPC 专用服务、NPC 专用回放格式、同一游戏流程维护两份代码

**NPC 类型枚举**:
`npcType` 按实体本质而不是职业、剧情身份或外观划分，只允许 `organic-sapient`（有机智慧生命）、`artificial-sapient`（机械或人工智慧）、`creature`（非人物型生物或造物）和 `incorporeal`（模因、能量或其他非实体存在）。无法以证据解决跨类争议的 NPC 不进入启用该字段的题池，不设置“其他”兜底。
_Avoid_: 职业作为类型、开放文本类型、用“其他”掩盖争议数据

**NPC 首次登场路径枚举**:
`debutRoute` 仅作为来源和策展备注，表示玩家首次能在游戏内实际遇见该 NPC 的内容类型；不进入 NPC v1.0 默认 GuessBoard。若未来启用，再使用开拓任务、开拓续闻、同行任务、冒险任务、限时活动、常驻独立玩法、世界探索或环境交互等封闭值。
_Avoid_: 把宣发时间当登场、自由文本任务类型、无证据猜测同版本先后

**NPC 专属字段反馈**:
NPC 类型和首次登场路径只返回 `exact`、`miss` 或异常保护用的 `unavailable`，不设 `close`；首次剧情登场版本使用有序比较，在不一致时提供答案更早或更晚的方向提示。
_Avoid_: 主观类型接近、任务类别接近、版本不提供方向

**NPC 非判题信息**:
Roadmap 中的“身份/子阵营”不得形成独立身份标签；可证明的组织归属并入主派系体系。剧情重要度主观性过高，不作为 v1.0 判题字段。二者可作为不参与比较的来源备注或内部策展依据。
_Avoid_: 主观重要度反馈、开放式职业身份枚举、同一组织同时作为身份和派系判题

**NPC 转为可玩角色**:
NPC 与可玩角色按题池隔离，而不是按全局人物身份强制互斥。某位 NPC 后续成为可玩角色时，默认从后续 NPC 答案池移出并作为独立可玩实体加入可玩角色池；确有玩法价值时可以在两个池分别保留，但不建立跨池人物映射。历史对局继续绑定旧 manifest。
_Avoid_: 同一局跨池比较、按名称隐式合并、为跨池身份建立映射系统

**NPC 形态合并**:
同一人物的换装、年龄阶段、投影和普通剧情形象合并为一个 NPC 实体，以别名、素材和来源记录差异；只有官方明确为独立人格或独立存在时才拆分。战斗首领形态不进入任何人物题池。
_Avoid_: 每张立绘一个答案、把战斗形态当 NPC、别名无法回溯到同一实体

**NPC 公开池层级**:
v1.0 只公开一个 `npc` 内容模式。内部只区分 `target` 与 `candidate-only`，答案从 `target` 列表均匀抽取；不维护核心/专家权重层，也不向玩家展示剧情重要度字段。
_Avoid_: 同时发布多个 NPC 模式、把策展等级当判题列、复杂权重系统

**NPC 同名实体**:
不同 NPC 在某种语言中同名时，只有同时具备独立官方条目、可区分头像和稳定来源 ID 才能保留。搜索按稳定实体 ID 返回并展示头像；猜错同名实体仍计为有效猜测，并明确提示选择的是另一同名 NPC。无法可靠区分的条目不得进入正式候选池。
_Avoid_: 用名称字符串判定、同名条目静默合并、同名猜测泄露答案字段

**星神图片猜测模式**:
星神不使用角色式属性 GuessBoard，采用独立图片辨认模式。玩家通过星神名称和别称搜索答案；最多 6 次猜测，每次猜错逐步揭示更多官方图片区域，继续复用现有猜测提交、结果、回放和分享流程。
_Avoid_: 用唯一命途直接泄题、为星神创造主观分类字段、显示完整图片后再猜

**星神题池范围**:
v1.0 收录官方星神菜单中的 18 个具名星神，排除泛用“星神”说明页。目标必须具备名称、可用别称搜索和可辨识官方图片；目标与候选均只来自星神题池。
_Avoid_: 把说明页当答案、混入普通角色或 NPC、无可辨识图片仍入目标池

**星神猜测反馈**:
猜测过程中只反馈猜中或猜错，不显示命途、命途行者、别称相似度或其它属性提示；每次猜错只增加图片揭示范围，结束后再展示完整星神资料。
_Avoid_: 猜错即显示唯一命途、按别称相似度提示、提前展示完整资料

**星神图片揭示规则**:
官方图片使用固定 `4×4` 方格遮罩，开局揭示 4 格。默认 6 猜时每次猜错增加 2 格；私人房选择 4 或 8 猜时，把后续 10 格均匀分配到最后一次猜测之前，使最终猜测前稳定显示 14 格。揭示顺序由对局 ID 确定，使同一场对局和历史回放保持一致；猜中或用尽次数后展示完整 16 格。前端直接使用遮罩实现，不预生成衍生图片。
_Avoid_: 每次刷新改变揭示顺序、参与同一挑战却看到不同线索、为每位星神人工制作多张阶段图

**星神模式入口**:
星神图片模式支持单人练习、私人房、好友挑战和周赛轮换；不进入每日题、实时随机匹配或正式 ELO。它使用独立 `modeId: "aeon"`，但继续复用现有对局创建、猜测、回放和分享流程。
_Avoid_: 18 个答案进入长期每日轮换、与普通角色玩家随机匹配、星神成绩计入正式 ELO

**星神图片裁切**:
每位星神配置一张官方主图和简单的 `focalPoint: { x, y }`。前端在固定画幅中围绕焦点裁切，再叠加通用 `4×4` 遮罩；18 张主图各人工检查一次，不制作或维护多套阶段图片。
_Avoid_: 所有图片盲目居中裁切、每个揭示阶段保存独立图片、运行时依赖人工重新构图

**小型消遣站复杂度边界**:
Fireflydle 优先保证题库公平、来源可追溯、历史版本可复现和核心流程稳定；不引入企业级审批、复杂反作弊、玩家长期行为画像、精细个性化权重或高成本多方审核。能由版本化数据、简单 CI 和人工清单解决的问题，不额外建立服务或流程。
_Avoid_: 为低风险小站建立重型审核系统、保存不必要的玩家行为历史、把内容策展做成后台平台

**世界观字段准入**:
题池启用地区或派系字段时，所有答案必须具备对应值；少量可猜候选允许显示 `unavailable`。如果缺失普遍到明显影响推理，则关闭该字段或缩小候选池，不设置固定覆盖率门槛。
_Avoid_: 答案关键字段缺失、正式题池大量 unavailable、为了达到 100% 覆盖过度缩池

**主叙事地区**:
唯一 `regionId` 表示实体在 Fireflydle 题目中的主叙事地区，按官方明确地区、主要剧情归属、首次核心登场地区的顺序审核；仍有争议时保持空值。出生地、临时到访地和当前所在地不自动覆盖主叙事地区。
_Avoid_: 出生地、当前位置、所有到访地、多值地区

**地区分类粒度**:
地区使用大型主叙事舞台层级，首发包括空间站「黑塔」、雅利洛-VI、仙舟、匹诺康尼、翁法罗斯及后续同级舞台。仙舟各舰船、贝洛伯格上下层等不拆为地区，必要差异由具体主派系表达；没有明确主舞台的实体不作为启用地区字段的目标。
_Avoid_: 把舰船或城区与世界级舞台混为同层、出生地覆盖剧情归属、地区与派系重复细分

**主派系精细度**:
普通世界观阵营固定为两层：`factionGroupId` 表示大势力体系，`factionId` 表示实体唯一的具体主派系。无法证明具体组织成员关系时使用经审核的较宽主派系，但不增加身份/羁绊第三层；特殊关系只作资料展示，不参与判题。
_Avoid_: 凭剧情印象细分、任意深度父子树、一个实体多个判题派系

**货币战争独立单位**:
货币战争使用完全独立的 `CurrencyWarsUnit` 数据集和稳定 `currencyWarsUnitId`，不保存普通 `GameEntity` 映射。名称、别名、头像、费用、站位和原生羁绊均从币战规则集自身读取；银狼 LV.999、泛称开拓者等直接作为独立币战单位处理。
_Avoid_: 映射到普通可玩角色、继承普通角色资料、按名称合并两个题池

**货币战争规则快照**:
每次发布记录一个简单的 `CurrencyWarsRuleset`，包含项目规则集 ID、对应游戏版本、抓取时间、来源 URL、羁绊和成员关系。发布后的对局绑定该规则集；不要求连续抓取、响应哈希或独立差异审核流水线。
_Avoid_: 运行时直连 Wiki 判题、原地修改历史规则集、瞬时接口结果直接发布

**货币战争字段启用门槛**:
每日题、普通角色模式和正式 ELO 不启用货币战争字段。独立 `currency-wars` 模式首发固定使用费用、站位和原生羁绊三项币战字段；定位/赋能等其它币战字段后置，只有完成归一化并确实提升玩法时再加入。
_Avoid_: 用 `unavailable` 填满币战题池、把普通角色字段带入币战、把未归一化的定位正文直接判题

**货币战争模式入口**:
货币战争是独立题池和模式入口，而不是普通模式 Modifier。它使用 `modeId: "currency-wars"`、独立字段配置和独立币战子池，但复用现有游戏创建、猜测、回放和分享流程。
_Avoid_: 普通模式临时切换字段、币战专用服务、币战与普通角色模式共用题池 ID

**货币战争对局范围**:
v1.0 的货币战争模式支持单人练习、私人房、好友挑战和周赛轮换；不进入每日题、普通角色实时随机匹配或正式 ELO。
_Avoid_: 币战污染每日题、与普通角色玩家随机匹配、币战成绩计入正式 ELO

**货币战争字段矩阵**:
货币战争模式使用独立的币战字段板，不显示普通属性、命途、稀有度、地区、版本或世界观阵营。v1.0 固定交付费用、站位和货币战争原生羁绊三项；定位/赋能等其它币战字段只有在后续数据足够稳定且确实提升玩法时再加入。两套分类不在同一 GuessBoard 中并列。
_Avoid_: 币战模式混用普通角色字段、把普通阵营改名成币战字段、在数据不足时强行加入定位

**货币战争字段完整子池**:
货币战争模式使用独立的币战单位子池，只收录费用、站位和原生羁绊都能正常判定的单位；具体规模按重新抓取和人工补齐后的实际结果确定，不设硬数字门槛。缺少任一首发字段的单位留在币战资料中，但不进入该模式目标和候选池。
_Avoid_: 为凑规模让字段缺失单位入池、在 GuessBoard 中大量显示 `unavailable`、把官方反向全集当成首发完整池

**货币战争单位准入**:
所有官方币战单位都可进入字段完整子池，包括模式专属形态、联动单位和泛称单位；是否与普通角色同名不影响币战准入，唯一条件是币战来源明确且费用、站位、原生羁绊三项完整。
_Avoid_: 用普通角色名单过滤币战单位、排除专属或联动单位、同名即合并

**货币战争猜测次数**:
货币战争模式固定最多 6 次猜测，不套用 NPC 模式的 4 猜规则；后续只根据实际试玩调整题池规则，不增加额外惩罚机制。
_Avoid_: 因专家主题默认 4 猜、额外生命或计时规则、按单位热度改变猜测次数

**货币战争专家顾问**:
专家顾问目前作为币战关系或类别保存，不进入 v1.0 角色 GuessBoard。只有官方未来将某位顾问作为费用、站位和羁绊完整的正式币战单位时，才按普通 `CurrencyWarsUnit` 准入。
_Avoid_: 顾问关系当角色字段、大量空费用或站位、单独建立顾问题池

**close 规则说明**:
`close` 单元格只显示统一黄色状态，不附带信息图标、逐格原因或可展开浮层。每个模式的规则页集中解释该模式下哪些情况属于 `close`，并提供必要示例；游戏引擎只返回判题状态及有实际玩法价值的方向信息。
_Avoid_: 黄色格逐项解释、引擎生成自然语言原因、网络响应携带币战羁绊交集

**模式规则入口**:
各模式共用一个可在开始前和对局中打开的“规则”按钮；桌面使用对话框，移动端使用底部面板。内容随题池切换，说明题池范围、猜测次数、当前字段、颜色和方向反馈，并给出少量固定示例。只有首次教学局主动引导，普通对局不自动弹出规则。
_Avoid_: 每种模式复制一套交互、规则只能开局前查看、每次进入都强制弹窗

**首次教学局**:
首次进入普通角色模式时提供可立即跳过的“10 秒教学”，使用固定答案和固定示例猜测一次展示绿、黄、灰及版本方向；完成或跳过后进入当天挑战。登录用户把完成状态记入账号，游客保存在本地；规则页可随时重新打开教学。NPC、货币战争和星神不各自复制教学流程。
_Avoid_: 强制完成后才能游玩、每种内容模式重复新手流程、跳过后无法重看

**结算推理复盘**:
普通角色、NPC 和货币战争在对局结束后按当局题池快照精确重放每次反馈，显示每猜之后的剩余候选数，并标出缩小候选范围最多的一猜；并列时取较早的一猜。进行中不返回候选数或复盘数据。星神图片模式不做字段候选复盘。
_Avoid_: 对局中显示剩余范围、用当前题库重算历史局、把图片揭示强行解释为字段信息量

**分享与好友挑战闭环**:
已完成单人对局的主要分享链接进入“挑战同一题”，固定相同答案、内容模式、规则、猜测次数和题库版本；分享内容只含无剧透结果图、简短成绩和挑战链接。移动端优先使用系统分享，不支持时提供复制链接与下载结果图；纯回放链接作为次要操作。
_Avoid_: 分享只打开首页、挑战双方使用不同题库、分享内容泄露答案

**好友挑战成绩锁定**:
每个账号在同一挑战中只有第一次完成的成绩参与对比，之后可以重玩练习但不能覆盖首次成绩。游客使用本地生成的访客 ID 记录；不增加设备指纹、IP 限制或复杂反作弊。
_Avoid_: 看到答案后覆盖正式成绩、游客无法参与、为异步娱乐挑战建立重型反作弊

**挑战与回放保留期**:
好友挑战链接保留 90 天，普通回放保留 30 天。过期页面只说明链接已过期并提供原内容模式入口，不恢复答案或成绩；题库 manifest 继续按仍有效的对局和历史统计需要保留。
_Avoid_: 过期链接泄露旧答案、挑战与回放无限增长、过期后只显示通用错误页

**PvP 双方同意跳过**:
实时随机匹配和双方同时在线的私人房允许任一方发起跳过请求；只有另一方确认后当前题才作废，双方都不得分且不计胜负，随后进入下一题。异步好友挑战链接不提供跳过，因为发起者已完成并锁定成绩，无法进行实时双边确认。
_Avoid_: 单方强制跳题、作废题影响胜负、异步挑战等待不存在的实时确认

**私人房基础设置**:
房主可选择内容模式（普通角色/NPC/货币战争/星神）、BO1/BO3/BO5/BO7、每题不限时或 30/60/90 秒，以及每题 4/6/8 次猜测；猜测次数默认使用该内容模式的正常值。开局后配置锁定，私人房成绩不计入 ELO。
_Avoid_: 把私人房猜测次数包装成公开难度层级、开局后单方改规则、私人规则污染正式 ELO

**私人房 Modifier**:
每个私人房最多启用一个玩法 Modifier，只允许“无”“极速”“迷雾”三选一，不允许叠加。极速适用于四种内容模式；迷雾只适用于普通角色、NPC 和货币战争字段棋盘，不用于星神图片模式。私人房不提供地区、版本、星级或其它专题筛选。
_Avoid_: 极速与迷雾叠加、星神模式强套字段迷雾、按专题创建大量小题池

**极速 Modifier**:
启用极速时房间必须选择 30/60/90 秒之一，不能不限时。每次猜错只从该玩家自己的剩余时间扣 5 秒；归零视为该题失败。猜对不额外加时，下一题按房间设置重置完整时间。
_Avoid_: 极速房不限时、猜错扣除对手时间、跨题累积剩余时间

**迷雾 Modifier**:
每题开始时随机选择一个判题字段并在整题中隐藏该列反馈；双方隐藏同一字段，字段名称仍显示，单元格使用统一迷雾状态。题目结束后在结算中恢复完整反馈，下一题重新抽取；不在每次猜测后改变隐藏字段。
_Avoid_: 双方隐藏不同字段、猜测过程中反复换列、结算仍不展示完整反馈

**无尽玩法资源规则**:
生存/连胜改为无尽玩法：开局 5 条命，不设整场总猜测次数上限，题目持续生成直到生命归零。每道题仍使用所选内容模式的正常单题次数上限；单题用尽次数仍未猜中时扣 1 条命并进入下一题。普通角色、NPC、货币战争和星神分别记录无尽成绩。
_Avoid_: 15 次整场猜测预算、单题真正无限导致生命无意义、不同内容模式成绩混排

**无尽玩法排名**:
无尽玩法取消独立的 3/2/1 积分，主成绩为本局完成的题目数；通关数相同时依次比较总猜测次数和用时。结算显示通关数、总猜测次数、历史最佳和全球百分位，四种内容模式分别排行。
_Avoid_: 通关更多却因积分更低而落后、不同模式共用榜单、结算同时展示两套主成绩

**无尽玩法抽题**:
同一次无尽挑战按开始时的题库 manifest 建立洗牌袋，每个答案在一轮内最多出现一次；全部用完后重新洗牌，并避免新一轮首题与上一题相同。题库更新不改变进行中的无尽挑战。
_Avoid_: 短时间重复答案、运行中热更新抽题池、题池耗尽后直接结束无尽玩法

**无尽玩法跳过**:
每次无尽挑战最多跳过 1 道题；跳过立即揭晓当前答案、扣 1 条命并进入下一题，不增加通关数。每日题和其它单人玩法不提供跳过。
_Avoid_: 无代价跳过、无限跳题刷取有利答案、每日题跳过

**货币战争版本数据存储**:
`CurrencyWarsUnit`、羁绊和成员关系全部保存在独立 `CurrencyWarsRuleset` 中，不与永久 `GameEntity` 关联。新版本新增一份静态规则快照，不改写旧币战单位或历史对局。
_Avoid_: 当前币战数据写入普通实体 payload、覆盖旧 ruleset、按名称运行时联结两个题池

## Confirmation

上述 v1.0 领域词汇、题池边界、模式矩阵、长期玩法、PWA 安装与离线边界、动效语言、数据稳定性和发布验收门槛已持续经产品方确认；最新确认日期为 2026-08-16，作为后续设计、实现和验收的共享基准。
